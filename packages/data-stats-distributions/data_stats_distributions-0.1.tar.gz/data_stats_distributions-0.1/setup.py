from setuptools import setup

setup(name='data_stats_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['data_stats_distributions'],
      zip_safe=False)

from setuptools import setup

setup(name='salemGaussianBinomial',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['salemGaussianBinomial'],
      zip_safe=False)

from setuptools import setup

setup(name='dsnd_distributions_nsaenzz',
      version='0.1',
      description='Gaussian distributions',
      packages=['dsnd_distributions_nsaenzz'],
      zip_safe=False)

from setuptools import setup

setup(name='random_var_distro',
      version='0.1',
      description='Probability distributions for continuous and discrete random variables',
      packages=['random_var_distro'],
      zip_safe=False)

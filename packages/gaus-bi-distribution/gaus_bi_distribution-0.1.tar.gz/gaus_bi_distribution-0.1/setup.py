from setuptools import setup

setup(name='gaus_bi_distribution',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['gaus_bi_distribution'],
      zip_safe=False)

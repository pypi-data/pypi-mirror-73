from setuptools import setup

setup(name='mr-distributions-test-mr',
      version='0.1',
      description='Gaussian distributions',
      packages=['mr-distributions-test-mr'],
      zip_safe=False)

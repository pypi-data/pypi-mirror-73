from setuptools import setup

setup(name='mk_distributions',
      version='0.2',
      description='Gaussian and Bionomial distributions by MK',
      packages=['mk_distributions'],
      zip_safe=False)

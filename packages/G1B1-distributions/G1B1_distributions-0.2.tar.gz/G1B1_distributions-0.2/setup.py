from setuptools import setup
from setuptools import find_packages

setup(name='G1B1_distributions',
      version='0.2',
      description='Gaussian distributions and Binomial distribution',
      packages=find_packages(),
      zip_safe=False)

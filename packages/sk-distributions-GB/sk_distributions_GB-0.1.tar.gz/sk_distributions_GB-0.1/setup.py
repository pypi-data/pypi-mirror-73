from setuptools import setup

setup(name='sk_distributions_GB',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['sk_distributions_GB'],
      zip_safe=False)

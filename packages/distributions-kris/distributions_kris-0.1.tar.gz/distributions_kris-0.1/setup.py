from setuptools import setup

setup(name='distributions_kris',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_kris'],
      author='Kris',
      author_email='example@example.com',
      zip_safe=False)

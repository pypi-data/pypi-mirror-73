from setuptools import setup

setup(name='bg_prob',
      version='0.4',
      description='Gaussian distributions',
      long_description=open('bg_prob/README.md').read(),
      packages=['bg_prob'],
      zip_safe=False)

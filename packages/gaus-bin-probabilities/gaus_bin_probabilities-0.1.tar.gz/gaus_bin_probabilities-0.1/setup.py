from setuptools import setup

setup(name='gaus_bin_probabilities',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['gaus_bin_probabilities'],
      zip_safe=False)

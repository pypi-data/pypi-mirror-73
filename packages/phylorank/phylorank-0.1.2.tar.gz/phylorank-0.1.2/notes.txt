- Species names can be problematic as different programs
  are inconsistent in how they handle binomial names.

  - PhyloRank assumes full species names with a space (not an underscore)
    used to seperate the 'specific epithet' from the 'specific name'

from setuptools import setup

setup(name='dsnd-anand-distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['dsnd-anand-distributions'],
      author=['Anand Nakat'],
      zip_safe=False)

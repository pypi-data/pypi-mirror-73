from setuptools import setup

setup(name='mbfirstpackage',
      version='0.1',
      description='My Gaussian and binomial distributions',
      packages=['mbfirstpackage'],
      zip_safe=False)
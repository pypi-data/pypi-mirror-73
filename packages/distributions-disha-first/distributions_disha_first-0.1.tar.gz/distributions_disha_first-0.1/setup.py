from setuptools import setup

setup(name='distributions_disha_first',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_disha_first'],
      zip_safe=False)

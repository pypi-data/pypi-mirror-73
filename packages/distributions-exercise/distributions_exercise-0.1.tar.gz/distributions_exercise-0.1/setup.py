from setuptools import setup

setup(name='distributions_exercise',
      version='0.1',
      description='Gaussian and binomial distributions',
      packages=['distributions_exercise'],
      zip_safe=False)

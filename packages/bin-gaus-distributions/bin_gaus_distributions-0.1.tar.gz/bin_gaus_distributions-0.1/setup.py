from setuptools import setup

setup(name='bin_gaus_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['bin_gaus_distributions'],
      zip_safe=False)

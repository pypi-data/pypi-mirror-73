from setuptools import setup

setup(name='maha-my-first-package',
      version='0.1',
      description='Gaussian distributions and Binomial distributions',
      packages=['maha-my-first-package'],
      zip_safe=False)

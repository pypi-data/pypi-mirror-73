from setuptools import setup

setup(name='learning_probability_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['learning_probability_distributions'],
      zip_safe=False)

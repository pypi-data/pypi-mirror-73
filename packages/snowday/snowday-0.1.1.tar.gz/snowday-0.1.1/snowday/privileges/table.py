from snowday.privileges.common import ALL, OWNERSHIP
from snowday.privileges.schema_objects import (
    SELECT,
    INSERT,
    UPDATE,
    DELETE,
    TRUNCATE,
    REFERENCES,
)


TABLE_PRIVILEGES = [
    ALL,
    SELECT,
    INSERT,
    UPDATE,
    DELETE,
    TRUNCATE,
    REFERENCES,
]

from setuptools import setup

setup(name='gb_distributions_test',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['gb_distributions_test'],
      author=['test'],
      zip_safe=False)

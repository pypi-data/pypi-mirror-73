#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

package_dir = \
{'': 'src'}

install_requires = \
['qtoml >= 0.3.0, < 1', 'toml >= 0.10.1, < 1']

entry_points = \
{'console_scripts': ['d2txt = d2txt:main']}

setup(name='d2txt',
      version='0.5.0',
      description="Parses Diablo II's TXT files and converts them to TOML files.",
      author='pastelmind',
      author_email='keepyourhonor@gmail.com',
      url='https://github.com/pastelmind/d2txt',
      py_modules=['d2txt'],
      package_dir=package_dir,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>= 3.6',
     )

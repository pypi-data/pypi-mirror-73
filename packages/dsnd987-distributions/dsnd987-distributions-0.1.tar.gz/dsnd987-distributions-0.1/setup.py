from setuptools import setup

setup(name='dsnd987-distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['dsnd987-distributions'],
      zip_safe=False)

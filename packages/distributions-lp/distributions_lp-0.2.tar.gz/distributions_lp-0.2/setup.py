from setuptools import setup

setup(name='distributions_lp',
      version='0.2',
      description='Gaussian distributions',
      packages=['distributions_lp'],
      author='itz_me_LP',
      zip_safe=False)

from setuptools import setup

setup(name='distributions_101',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_101'],
      zip_safe=False)
